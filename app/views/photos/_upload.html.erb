<%= form_for [@profile, Photo.new] do |f| %>
  <div class="field">
  	<%= f.label :avatar, "Select a photo to upload from your computer" %>
  	<%= f.file_field :avatar %>
  </div>
  <div class="field">
  	<%= f.label :primary, "Make this photo your main profile picture?" %>
  	<%= f.check_box :primary %>
  </div>
  <div class="actions">
  	<%= f.submit %>
  </div>
<% end %>